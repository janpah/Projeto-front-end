import { HoverSizeDirective } from './hover-size.directive';

describe('HoverSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverSizeDirective();
    expect(directive).toBeTruthy();
  });
});
